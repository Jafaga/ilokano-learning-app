# GitHub Workflow

## Recommended Branches

```txt
main
dev
feature/welcome-screen
feature/lesson-flow
feature/progress-storage
feature/supabase-auth
feature/content-dashboard
```

## First Commit

```bash
git init
git add .
git commit -m "Initial Next.js Ilokano learning app MVP"
```

## Example Commit Messages

```bash
git commit -m "Add mobile-friendly welcome screen"
git commit -m "Create sample Ilokano lesson data"
git commit -m "Build lesson answer checking"
git commit -m "Save progress with localStorage"
git commit -m "Add Supabase schema"
```

## Suggested GitHub Issues

1. Set up Next.js project.
2. Build welcome page.
3. Build home/course path page.
4. Create sample lesson data.
5. Add multiple-choice question support.
6. Add translation question support.
7. Save XP and progress.
8. Add Supabase authentication.
9. Move lessons into database.
10. Create contributor content workflow.
